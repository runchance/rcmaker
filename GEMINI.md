# rcmaker Project Instructions

Before changing this project, read and follow `AGENTS.md` and `.agents/skills/rcmaker-development/SKILL.md`. Then open the task-specific references selected by the Skill and the corresponding framework documentation in `.agents/doc/`.

The framework reuse rules are mandatory: do not duplicate existing rcmaker components, and keep Windows development code compatible with the configured Linux multi-process production environment.
